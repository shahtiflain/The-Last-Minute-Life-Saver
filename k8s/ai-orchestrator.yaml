apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hustlr-ai-orchestrator
spec:
  template:
    spec:
      containers:
      - image: ai-image
        ports:
        - containerPort: 8000
        env:
        - name: NODE_ENV
          value: "production"
