apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hustlr-frontend
spec:
  template:
    spec:
      containers:
      - image: frontend-image
        ports:
        - containerPort: 8080
        env:
        - name: NODE_ENV
          value: "production"
